

exports.perimeter = (breadth, length) => {
    return ("Perimeter of rectangle is " + 2*(length*breadth))
}
const area = (length, breadth) => {
    return ("Area of rectangle is " + (lenght*breadth))
}


// exports.perimeter = getRectanlgPerimeter
// exports.
